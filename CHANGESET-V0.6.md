# V0.7 统计说明、自动版本与变更闭环

## 统计口径说明

- 仪表盘6个统计卡片和5个图表/列表模块均增加说明图标。
- 完整度分析6个指标卡片和4个分析模块均增加说明图标。
- 鼠标悬停或键盘聚焦说明图标时，展示统计范围、状态条件、计算公式和排除规则。
- 说明内容与当前后端SQL统计规则保持一致。

## 交付物编码说明

- 台账“交付物编码”表头增加编码规则说明。
- 当前编码结构：`AD-部门编码-类型编码-项目编码-对象编码-三位流水号`。
- 编码创建后不可手工修改，归档不会释放原编码。

## 三级自动版本号

内部版本号统一为`V主版本.次版本.修订号`：

- 重大版本：主版本号+1，其他归零。
- 功能版本：次版本号+1，修订号归零。
- 修订版本：修订号+1。
- 首个版本固定为`V1.0.0`。

新增版本时用户只选择版本类型，后端扫描该交付物现有合法版本并计算最高版本的下一个版本号。提交时再次计算；遇到并发版本号冲突会自动重新计算一次。

## 变更与交付物版本闭环

- 发起变更时自动锁定交付物当前有效版本为变更前版本。
- 变更批准并开始实施后，可直接创建变更版本。
- 变更版本创建时自动关联`ToVersionId`。
- 未创建变更版本不能提交验证。
- 变更版本未正式发布不能关闭变更。
- 变更版本发布后成为交付物当前版本，原当前版本标记为已替代。
- 台账操作列增加“变更记录”，右侧抽屉按更新时间由近到远展示完整时间线。

## V0.6基础能力

- 修复版本和变更审批按钮路由。
- CSV可选字段导出。
- 交付物关联关系。
- 完整度分析。
- 用户、角色、登录和用户删除。

## 构建验证

正式Action已增加：

- 全部前端JavaScript语法检查。
- .NET 10依赖还原。
- Release编译。
- Windows x64自包含发布。
- 构建摘要和发布包上传。
