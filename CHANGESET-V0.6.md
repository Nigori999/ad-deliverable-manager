# V0.6 管理能力升级

## 修复

- 将版本“提交审批、退回、发布、废止”改为自定义确认窗口和明确的处理中、成功、失败反馈。
- 将变更“批准、驳回、开始实施、提交验证、关闭”改为可靠的异步操作反馈。
- 静态资源增加版本号和禁止缓存响应，避免浏览器继续使用旧JavaScript导致按钮无响应。
- 后端状态更新增加前置状态条件，防止重复点击或多人并发时覆盖状态。
- 版本发布必须经过审批中状态，并记录审批人和处理意见。
- 变更批准、驳回必须填写评审意见，并写入操作日志。

## 新增

### CSV导出

- 交付物台账和变更管理均支持CSV导出。
- 导出前可以选择字段。
- 交付物导出沿用当前查询条件，变更导出沿用状态筛选。
- CSV使用UTF-8 BOM，并处理逗号、换行、引号和公式注入风险。

### 交付物关联

- 支持派生、验证、依赖、引用、替代五种关系。
- 支持交付物级关联和指定版本关联。
- 交付物详情页展示上游、下游关系。
- 支持创建、查看和删除关系，并记录审计日志。

### 完整度分析

- 公共字段和不同交付物类型专属字段完整度。
- 各部门完整度。
- PRD到FR的派生追溯完整度。
- FR到测试用例的验证追溯完整度。
- 项目硬件软件包覆盖情况。
- 待审批版本、未关闭变更、无当前版本和超过90天未更新的数据提醒。
- 可从问题清单直接跳转到交付物详情。

### 用户与角色

固定四类角色：

- ADMIN：管理员，拥有全部权限。
- EDITOR：编辑者，负责交付物录入、版本提交、关联维护和变更实施。
- APPROVER：审批者，负责版本发布/退回/废止和变更批准/驳回/关闭。
- VIEWER：查看者，只读和导出。

同时新增：

- 首次启动创建管理员。
- 登录、退出和修改密码。
- 管理员新增用户、修改角色、启停账号和重置密码。
- 首次登录强制修改密码。
- PBKDF2-SHA256密码散列，不保存明文密码。
- 角色或账号状态变化后，原登录会话自动失效。

## 数据库升级

- 新增`Users`表。
- 新增用户和交付物关联索引。
- 使用`CREATE TABLE/INDEX IF NOT EXISTS`幂等升级，不删除现有交付物、版本和变更数据。

## 验证

已完成：

- 全部前端JavaScript语法检查。
- 新数据库初始化测试。
- 旧版数据库结构升级测试。
- 初始化脚本重复执行测试。
- 关联、追溯、CSV查询和生命周期SQL测试。

仍需通过GitHub Actions或Windows开发机完成.NET 10编译，以及在发布包中完成端到端操作验证。
