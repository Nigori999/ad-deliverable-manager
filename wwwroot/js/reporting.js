const dashboardRules={totalDeliverables:'统计对象：生命周期状态为“有效”的交付物主档。排除已归档交付物。',currentVersions:'统计对象：当前版本且已发布的版本。',pendingReview:'统计版本状态为审批中的版本。',monthlyNewVersions:'按版本创建时间统计当前自然月新增版本。',monthlyChanges:'按变更记录创建时间统计当前自然月变更。',deprecatedVersions:'统计全部已废止版本。',departmentDistribution:'按部门统计有效交付物主档数量。',statusDistribution:'统计全部版本的当前状态分布。',monthlyTrend:'展示最近6个自然月版本与变更趋势。',typeDistribution:'按交付物类型统计有效交付物数量。',recent:'按版本最近更新时间倒序展示前8条。'};
const analyticsRules={metadata:'统计全部有效交付物的必填元数据完整度，交付物类别属于主档必填项。',prdTrace:'统计有效PRD是否建立PRD→FR派生关系。',frTrace:'统计有效FR是否建立FR→测试用例验证关系。',pendingReview:'统计审批中的版本。',pendingChanges:'统计未关闭且未驳回的变更。',stale:'统计超过90天未更新的有效交付物。',department:'按部门统计元数据完整度。',traceability:'统计PRD→FR及FR→测试用例追溯覆盖。',hardware:'以“基础设置”中当前启用的硬件软件包类别为标准，统计各项目正式发布软件包覆盖情况。',issues:'列出必填字段缺失或超过90天未更新的有效交付物。'};
function infoTip(text){const value=esc(text).replaceAll('\n','&#10;');return `<span class="info-tip" tabindex="0" aria-label="${value}" title="${value}" data-tooltip="${value}">i</span>`;}function titleWithTip(title,rule){return `<span class="title-with-tip">${esc(title)}${infoTip(rule)}</span>`;}function statCardWithRule(label,value,unit,rule){return `<div class="stat-card"><span class="stat-label-with-tip">${esc(label)}${infoTip(rule)}</span><strong>${Number(value||0)}</strong><small>${esc(unit)}</small></div>`;}function metricCardWithRule(label,value,note,rule,percent=null){return `<div class="stat-card"><span class="stat-label-with-tip">${esc(label)}${infoTip(rule)}</span><strong>${esc(value)}</strong><small>${esc(note)}</small>${percent===null?'':`<div class="compact-progress"><span style="width:${Math.max(0,Math.min(100,percent))}%"></span></div>`}</div>`;}
async function renderDashboard(){setPage('仪表盘','交付物、版本和变更状态总览');const data=await api('/internal/dashboard'),s=data.summary;content.innerHTML=`<section class="stat-grid">${statCardWithRule('交付物总数',s.totalDeliverables,'项',dashboardRules.totalDeliverables)}${statCardWithRule('当前有效版本',s.currentVersions,'个',dashboardRules.currentVersions)}${statCardWithRule('待评审版本',s.pendingReview,'个',dashboardRules.pendingReview)}${statCardWithRule('本月新增版本',s.monthlyNewVersions,'个',dashboardRules.monthlyNewVersions)}${statCardWithRule('本月变更',s.monthlyChanges,'项',dashboardRules.monthlyChanges)}${statCardWithRule('已废止版本',s.deprecatedVersions,'个',dashboardRules.deprecatedVersions)}</section><section class="dashboard-grid"><div class="card"><div class="card-head"><h3>${titleWithTip('各部门交付物数量',dashboardRules.departmentDistribution)}</h3></div><div class="card-body"><canvas id="department-chart" class="chart"></canvas></div></div><div class="card"><div class="card-head"><h3>${titleWithTip('版本状态分布',dashboardRules.statusDistribution)}</h3></div><div class="card-body"><canvas id="status-chart" class="chart"></canvas><div id="status-legend" class="chart-legend"></div></div></div></section><section class="card" style="margin-bottom:18px"><div class="card-head"><h3>${titleWithTip('近6个月版本与变更趋势',dashboardRules.monthlyTrend)}</h3></div><div class="card-body"><canvas id="trend-chart" class="chart"></canvas></div></section><section class="dashboard-grid"><div class="card"><div class="card-head"><h3>${titleWithTip('交付物类型分布',dashboardRules.typeDistribution)}</h3></div><div class="card-body"><canvas id="type-chart" class="chart"></canvas></div></div><div class="card"><div class="card-head"><h3>${titleWithTip('最近更新',dashboardRules.recent)}</h3><a class="btn btn-light btn-sm" href="#/deliverables">查看台账</a></div><div class="card-body recent-list">${data.recent.length?data.recent.map(x=>`<a class="recent-row" href="#/deliverables/${x.id}"><div><strong>${esc(x.name)}</strong><small>${esc(x.code)} · ${esc(x.version)}</small></div>${statusBadge(x.status)}</a>`).join(''):'<div class="empty">暂无交付物版本</div>'}</div></div></section>`;requestAnimationFrame(()=>{drawBarChart(byId('department-chart'),data.departmentDistribution);drawDonutChart(byId('status-chart'),data.statusDistribution,byId('status-legend'));drawLineChart(byId('trend-chart'),data.monthlyTrend);drawBarChart(byId('type-chart'),data.typeDistribution);});}
async function renderAnalytics(){setPage('完整度分析','识别元数据缺失、追溯断点、硬件覆盖和待处理事项');const data=await api('/internal/analytics/completeness'),s=data.summary;content.innerHTML=`<section class="stat-grid analytics-stats">${metricCardWithRule('元数据完整度',`${s.metadataPercent}%`,`${s.completeDeliverables}/${s.deliverables} 项完全完整`,analyticsRules.metadata,s.metadataPercent)}${metricCardWithRule('PRD→FR追溯',`${s.prdTracePercent}%`,'已建立派生关系',analyticsRules.prdTrace,s.prdTracePercent)}${metricCardWithRule('FR→测试用例',`${s.frTestTracePercent}%`,'已建立验证关系',analyticsRules.frTrace,s.frTestTracePercent)}${metricCardWithRule('待审批版本',s.pendingReview,'需要审批者处理',analyticsRules.pendingReview)}${metricCardWithRule('未关闭变更',s.pendingChanges,'待评估/实施/验证',analyticsRules.pendingChanges)}${metricCardWithRule('超90天未更新',s.stale,'建议确认有效性',analyticsRules.stale)}</section><section class="dashboard-grid"><div class="card"><div class="card-head"><h3>${titleWithTip('部门元数据完整度',analyticsRules.department)}</h3></div><div class="card-body progress-list">${data.departmentCompleteness.map(x=>progressRow(x.name,x.percent,`${x.complete}/${x.total} 项完全完整`)).join('')||'<div class="empty">暂无数据</div>'}</div></div><div class="card"><div class="card-head"><h3>${titleWithTip('需求追溯完整度',analyticsRules.traceability)}</h3></div><div class="card-body progress-list">${progressRow('PRD → FR',data.traceability.prdToFr.percent,`${data.traceability.prdToFr.linked}/${data.traceability.prdToFr.total} 个PRD已关联FR`)}${progressRow('FR → 测试用例',data.traceability.frToTestCase.percent,`${data.traceability.frToTestCase.linked}/${data.traceability.frToTestCase.total} 个FR已关联测试用例`)}</div></div></section><section class="card" style="margin-bottom:18px"><div class="card-head"><h3>${titleWithTip('项目硬件软件包覆盖',analyticsRules.hardware)}</h3><span class="muted">按基础设置中的 ${data.hardwareCategoryCount} 个启用类别检查</span></div><div class="table-wrap">${data.hardwareCoverage.length?`<table><thead><tr><th>项目</th><th>覆盖率</th><th>已覆盖</th><th>缺失类别</th></tr></thead><tbody>${data.hardwareCoverage.map(x=>`<tr><td><strong>${esc(x.projectName)}</strong><div class="muted">${esc(x.projectCode)}</div></td><td><div class="compact-progress"><span style="width:${x.percent}%"></span></div><strong>${x.percent}%</strong></td><td>${esc(x.covered)} / ${esc(x.expected)}</td><td>${x.missing.length?x.missing.map(v=>`<span class="badge deprecated">${esc(v)}</span>`).join(' '):'<span class="badge released">完整</span>'}</td></tr>`).join('')}</tbody></table>`:'<div class="empty">暂无项目数据</div>'}</div></section><section class="card"><div class="card-head"><h3>${titleWithTip('数据问题清单',analyticsRules.issues)}</h3><span class="muted">最多显示100项</span></div><div class="table-wrap">${data.issues.length?`<table><thead><tr><th>交付物</th><th>类型 / 类别</th><th>项目</th><th>完整度</th><th>缺失字段</th><th>最近更新</th><th>操作</th></tr></thead><tbody>${data.issues.map(x=>`<tr><td><strong>${esc(x.name)}</strong><div class="code">${esc(x.code)}</div></td><td>${esc(x.type)}<div class="muted">${esc(x.category)}</div></td><td>${esc(x.project)}</td><td><strong>${x.percent}%</strong></td><td class="tag-list">${x.missing.map(v=>`<span class="badge pending_assessment">${esc(v)}</span>`).join(' ')||'<span class="badge released">完整</span>'}</td><td>${esc(fmtDate(x.updatedAt))}</td><td><a class="btn btn-light btn-sm" href="#/deliverables/${x.id}">查看详情</a></td></tr>`).join('')}</tbody></table>`:'<div class="empty">没有发现缺失或超期数据</div>'}</div></section>`;}
