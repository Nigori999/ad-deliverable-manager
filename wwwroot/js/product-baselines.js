const baselinePermissionCodes=['BASELINE_VIEW','BASELINE_CREATE','BASELINE_EDIT','BASELINE_PUBLISH','BASELINE_COPY','BASELINE_CHANGE'];
if(typeof permissionCategories!=='undefined'){permissionCategories['产品版本基线']=baselinePermissionCodes;Object.assign(permissionFriendly,{BASELINE_VIEW:'查看产品基线',BASELINE_CREATE:'新增产品基线',BASELINE_EDIT:'编辑基线草稿',BASELINE_PUBLISH:'发布产品基线',BASELINE_COPY:'复制已发布基线',BASELINE_CHANGE:'变更基线基础信息'});}

const baselineStatusNames={DRAFT:'草稿',RELEASED:'已发布'};
const baselineRoleNames={PRD:'PRD',FR:'FR',TC:'测试用例',TEST_REPORT:'测试报告'};
const baselineEsc=typeof esc==='function'?esc:(v=>String(v??''));
function baselineStatusBadge(s){return `<span class="badge ${String(s||'').toLowerCase()}">${baselineEsc(baselineStatusNames[s]||s)}</span>`;}

async function renderProductBaselines(){
  setPage('产品版本基线','管理智驾产品版本、硬件软件包快照及可选交付物');
  const data=await api('/internal/product-baselines');
  const canCreate=hasPermission('BASELINE_CREATE');
  content.innerHTML=`<section class="card"><div class="card-head"><div><h3>产品版本基线</h3><p class="muted section-note">已发布基线不可直接修改；后续产品版本只能从已发布基线复制创建。</p></div>${canCreate?'<button type="button" id="baseline-create" class="btn btn-primary">+ 新建产品版本</button>':''}</div><div class="table-wrap">${data.items.length?`<table><thead><tr><th>产品</th><th>产品版本</th><th>状态</th><th>车型</th><th>创建人</th><th>发布时间</th><th>操作</th></tr></thead><tbody>${data.items.map(x=>`<tr><td>${baselineEsc(x.productName)}</td><td class="code">${baselineEsc(x.version)}</td><td>${baselineStatusBadge(x.status)}</td><td>${baselineEsc(x.vehicleModels||'—')}</td><td>${baselineEsc(x.createdBy)}</td><td>${baselineEsc(x.releaseDate?fmtDate(x.releaseDate):'—')}</td><td><div class="inline-actions"><button type="button" class="btn btn-light btn-sm" data-baseline-detail="${x.id}">详情</button>${x.status==='DRAFT'&&hasPermission('BASELINE_EDIT')?`<button type="button" class="btn btn-light btn-sm" data-baseline-edit="${x.id}">编辑</button>`:''}${x.status==='DRAFT'&&hasPermission('BASELINE_PUBLISH')?`<button type="button" class="btn btn-primary btn-sm" data-baseline-publish="${x.id}">发布</button>`:''}${x.status==='RELEASED'&&hasPermission('BASELINE_COPY')?`<button type="button" class="btn btn-light btn-sm" data-baseline-copy="${x.id}">复制</button>`:''}${x.status==='RELEASED'&&hasPermission('BASELINE_CHANGE')?`<button type="button" class="btn btn-light btn-sm" data-baseline-change="${x.id}">基础信息变更</button>`:''}</div></td></tr>`).join('')}</tbody></table>`:'<div class="empty">还没有产品版本基线。创建第一个产品版本后，后续版本建议从已发布基线复制。</div>'}</div></section>`;
  const bind=(selector,fn)=>content.querySelectorAll(selector).forEach(btn=>btn.addEventListener('click',()=>fn(Number(btn.dataset[selector.replace(/\[|\]|data-baseline-/g,'')]))));
  if(canCreate)byId('baseline-create').onclick=()=>openBaselineCreate();
  content.querySelectorAll('[data-baseline-detail]').forEach(b=>b.onclick=()=>openBaselineDetail(Number(b.dataset.baselineDetail)));
  content.querySelectorAll('[data-baseline-edit]').forEach(b=>b.onclick=()=>openBaselineEditor(Number(b.dataset.baselineEdit)));
  content.querySelectorAll('[data-baseline-publish]').forEach(b=>b.onclick=()=>publishBaseline(Number(b.dataset.baselinePublish)));
  content.querySelectorAll('[data-baseline-copy]').forEach(b=>b.onclick=()=>copyBaseline(Number(b.dataset.baselineCopy)));
  content.querySelectorAll('[data-baseline-change]').forEach(b=>b.onclick=()=>changeBaseline(Number(b.dataset.baselineChange)));
}

function baselineModalForm(detail,options){
  const hardware=detail?.hardware||[];
  const docs=detail?.deliverables||[];
  const hardwareOptions=options.hardware||[];
  const documentOptions=options.documents||[];
  const hardwareRows=hardware.length?hardware.map((x,i)=>baselineHardwareRow(i,x.hardwareCategory,x.hardwareModel,x.softwareVersionId,hardwareOptions)).join(''):baselineHardwareRow(0,'','',0,hardwareOptions);
  const docRows=docs.filter(x=>['PRD','FR','TC','TEST_REPORT'].includes(x.roleCode)).map(x=>baselineDocumentSelect(x.roleCode,x.versionId,documentOptions)).join('');
  return `<form id="baseline-form"><div class="form-grid"><div class="field"><label>产品名称 *</label><input name="productName" value="${baselineEsc(detail?.baseline?.productName||'智驾产品')}" required maxlength="80"></div><div class="field"><label>产品版本</label><input value="${baselineEsc(detail?.baseline?.internalVersion||'V1.0.0')}" disabled></div></div><div class="field"><label>说明</label><textarea name="description" rows="3">${baselineEsc(detail?.baseline?.description||'')}</textarea></div><div class="form-grid"><div class="field"><label>车型</label><input name="vehicleModels" value="${baselineEsc(detail?.baseline?.vehicleModels||'')}" placeholder="如：A10、A10 Pro"></div><div class="field"><label>ODD</label><textarea name="odd" rows="3" placeholder="道路、天气、速度、区域等">${baselineEsc(detail?.baseline?.odd||'')}</textarea></div></div><div class="field"><label>智驾能力</label><textarea name="capabilities" rows="3" placeholder="如：高速NOA、城区NOA、APA">${baselineEsc(detail?.baseline?.capabilities||'')}</textarea></div><div class="section-divider"><div class="card-head"><div><h4>硬件配置及对应软件包 *</h4><p class="form-hint">每个硬件类别都必须绑定一个已正式发布或已替代的软件包版本。</p></div><button type="button" class="btn btn-light btn-sm" id="add-baseline-hardware">+ 添加硬件</button></div><div id="baseline-hardware-list">${hardwareRows}</div></div><div class="section-divider"><h4>可选交付物</h4><p class="form-hint">PRD、FR、测试用例、测试报告均为辅助资料，不影响基线发布。</p><div id="baseline-document-list">${docRows}${baselineDocumentSelect('PRD',0,documentOptions,true)}${baselineDocumentSelect('FR',0,documentOptions,true)}${baselineDocumentSelect('TC',0,documentOptions,true)}${baselineDocumentSelect('TEST_REPORT',0,documentOptions,true)}</div></div><input type="hidden" name="revision" value="${detail?.baseline?.revision||1}"></form>`;
}
function baselineHardwareRow(index,category='',model='',versionId=0,options=[]){return `<div class="form-grid baseline-hardware-row" data-index="${index}"><div class="field"><label>硬件类别 *</label><input name="hardwareCategory" value="${baselineEsc(category)}" required placeholder="如：智驾域控、摄像头、激光雷达"></div><div class="field"><label>硬件型号</label><input name="hardwareModel" value="${baselineEsc(model)}"></div><div class="field"><label>软件包版本 *</label><select name="softwareVersionId" required><option value="">请选择已发布软件包</option>${options.map(x=>`<option value="${x.id}" ${Number(x.id)===Number(versionId)?'selected':''}>${baselineEsc(`${x.hardwareCategory||'硬件软件包'} / ${x.name} / ${x.version}`)}</option>`).join('')}</select></div><div class="field" style="display:flex;align-items:end"><button type="button" class="btn btn-light btn-sm remove-baseline-hardware">删除</button></div></div>`;}
function baselineDocumentSelect(role,versionId,options,hideIfEmpty=false){const list=role==='PRD'?options.filter(x=>x.typeCode==='PRD'):role==='FR'?options.filter(x=>x.typeCode==='FR'):role==='TC'?options.filter(x=>x.typeCode==='TC'):options;return `<div class="field"><label>${baselineRoleNames[role]||role}</label><select name="doc_${role}"><option value="">未关联</option>${list.map(x=>`<option value="${x.id}" ${Number(x.id)===Number(versionId)?'selected':''}>${baselineEsc(`${x.name} / ${x.version}`)}</option>`).join('')}</select></div>`;}

async function openBaselineCreate(){showModal('新建产品版本基线',`<form id="baseline-create-form"><div class="field"><label>产品名称 *</label><input name="productName" required maxlength="80" placeholder="如：智驾产品"></div><div class="form-hint">首个产品版本基线自动使用 V1.0.0；发布后可复制创建 V1.0.1、V1.1.0 或 V2.0.0。</div><div class="field"><label>说明</label><textarea name="description" rows="3"></textarea></div></form>`,{submitText:'创建草稿',onSubmit:async close=>{const form=byId('baseline-create-form');if(!form.reportValidity())throw new Error('请填写产品名称。');const fd=new FormData(form);const result=await api('/internal/product-baselines',{method:'POST',body:JSON.stringify({productName:fd.get('productName'),description:fd.get('description'),vehicleModels:'',odd:'',capabilities:''})});close();toast('产品基线草稿已创建。');if(result?.id)await openBaselineEditor(Number(result.id));}});}

async function openBaselineEditor(id){const detail=await api(`/internal/product-baselines/${id}`);if(detail.baseline.versionStatus!=='DRAFT')return toast('已发布基线不能直接编辑，请通过基础信息变更处理。','error');const options=await api('/internal/product-baselines/options');showModal(`编辑 ${detail.baseline.internalVersion}`,baselineModalForm(detail,options),{submitText:'保存草稿',onSubmit:async close=>{const form=byId('baseline-form');if(!form.reportValidity())throw new Error('请补全必填信息。');const hardware=[...form.querySelectorAll('.baseline-hardware-row')].map(row=>({hardwareCategory:row.querySelector('[name="hardwareCategory"]').value.trim(),hardwareModel:row.querySelector('[name="hardwareModel"]').value.trim(),softwareVersionId:Number(row.querySelector('[name="softwareVersionId"]').value)}));if(hardware.some(x=>!x.hardwareCategory||!x.softwareVersionId))throw new Error('每个硬件都必须配置类别和软件包版本。');const deliverables=[];for(const role of ['PRD','FR','TC','TEST_REPORT']){const value=form.querySelector(`[name="doc_${role}"]`)?.value;if(value)deliverables.push({roleCode:role,versionId:Number(value)});}await api(`/internal/product-baselines/${id}`,{method:'PUT',body:JSON.stringify({productName:form.elements.productName.value,description:form.elements.description.value,vehicleModels:form.elements.vehicleModels.value,odd:form.elements.odd.value,capabilities:form.elements.capabilities.value,hardware,deliverables,revision:Number(form.elements.revision.value)})});close();toast('基线草稿已保存。');await renderProductBaselines();}});setTimeout(()=>{const list=byId('baseline-hardware-list');byId('add-baseline-hardware').onclick=()=>{const index=list.querySelectorAll('.baseline-hardware-row').length;list.insertAdjacentHTML('beforeend',baselineHardwareRow(index,'','',0,options.hardware));bindHardwareButtons();};function bindHardwareButtons(){list.querySelectorAll('.remove-baseline-hardware').forEach(btn=>btn.onclick=()=>{if(list.querySelectorAll('.baseline-hardware-row').length<=1)return toast('至少保留一项硬件配置。','error');btn.closest('.baseline-hardware-row').remove();});}bindHardwareButtons();},0);}

async function openBaselineDetail(id){const detail=await api(`/internal/product-baselines/${id}`);const b=detail.baseline;showModal(`产品基线 ${b.internalVersion}`,`<div class="detail-grid"><div><strong>产品名称</strong><span>${baselineEsc(b.productName)}</span></div><div><strong>状态</strong><span>${baselineStatusBadge(b.versionStatus)}</span></div><div><strong>车型</strong><span>${baselineEsc(b.vehicleModels||'—')}</span></div><div><strong>ODD</strong><span>${baselineEsc(b.odd||'—')}</span></div><div><strong>智驾能力</strong><span>${baselineEsc(b.capabilities||'—')}</span></div><div><strong>说明</strong><span>${baselineEsc(b.description||'—')}</span></div></div><div class="section-divider"><h4>硬件及软件包</h4>${detail.hardware.length?`<div class="table-wrap"><table><thead><tr><th>硬件类别</th><th>型号</th><th>软件包</th><th>版本</th></tr></thead><tbody>${detail.hardware.map(x=>`<tr><td>${baselineEsc(x.hardwareCategory)}</td><td>${baselineEsc(x.hardwareModel||'—')}</td><td>${baselineEsc(x.softwareName)}</td><td class="code">${baselineEsc(x.softwareVersion)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">未配置硬件。</div>'}</div><div class="section-divider"><h4>辅助交付物</h4>${detail.deliverables.length?`<div class="table-wrap"><table><thead><tr><th>类型</th><th>交付物</th><th>版本</th></tr></thead><tbody>${detail.deliverables.map(x=>`<tr><td>${baselineEsc(baselineRoleNames[x.roleCode]||x.typeName)}</td><td>${baselineEsc(x.name)}</td><td class="code">${baselineEsc(x.version)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">未关联PRD、FR、测试用例或测试报告。</div>'}</div><div class="section-divider"><h4>基础信息变更记录</h4>${detail.changes.length?detail.changes.map(x=>`<div class="timeline-item"><strong>${baselineEsc(x.changeReason)}</strong><div class="muted">${baselineEsc(x.operatorName)} · ${baselineEsc(fmtDate(x.createdAt))}</div><div>${baselineEsc(x.description||'—')}</div></div>`).join(''):'<div class="empty">暂无变更记录。</div>'}</div>`,{submitText:'关闭',onSubmit:async close=>close()});}

async function publishBaseline(id){const detail=await api(`/internal/product-baselines/${id}`);const result=await confirmAction('正式发布产品基线',`发布 ${detail.baseline.productName} ${detail.baseline.internalVersion} 后将不能直接修改组成信息，只能复制新版本或通过变更修改基础信息。`);if(!result.confirmed)return;await api(`/internal/product-baselines/${id}/publish`,{method:'POST',body:JSON.stringify({revision:detail.baseline.revision})});toast('产品版本基线已正式发布。');await renderProductBaselines();}

async function copyBaseline(id){const result=await showBaselineVersionTypeModal();if(!result)return;const created=await api(`/internal/product-baselines/${id}/copy`,{method:'POST',body:JSON.stringify({versionType:result})});toast('已创建新的产品基线草稿。');await openBaselineEditor(Number(created.id));}
function showBaselineVersionTypeModal(){return new Promise(resolve=>{showModal('选择新产品版本号',`<div class="field"><label>版本类型</label><select id="baseline-version-type"><option value="PATCH">修订版本（Patch）</option><option value="MINOR" selected>功能版本（Minor）</option><option value="MAJOR">重大版本（Major）</option></select><div class="form-hint">系统会基于当前已发布版本自动生成新版本号。</div></div>`,{submitText:'继续',onSubmit:async close=>{const value=byId('baseline-version-type').value;close();resolve(value);}});});}

async function changeBaseline(id){const detail=await api(`/internal/product-baselines/${id}`);const b=detail.baseline;showModal('变更基线基础信息',`<form id="baseline-change-form"><div class="notice-panel">已发布基线的硬件软件包、PRD、FR、测试用例和测试报告引用不可通过此变更修改；本操作仅修改车型、ODD、能力和说明。</div><div class="field"><label>变更原因 *</label><textarea name="changeReason" rows="3" required></textarea></div><div class="field"><label>变更说明</label><textarea name="description" rows="3">${baselineEsc(b.description)}</textarea></div><div class="field"><label>车型</label><input name="vehicleModels" value="${baselineEsc(b.vehicleModels)}"></div><div class="field"><label>ODD</label><textarea name="odd" rows="3">${baselineEsc(b.odd)}</textarea></div><div class="field"><label>智驾能力</label><textarea name="capabilities" rows="3">${baselineEsc(b.capabilities)}</textarea></div></form>`,{submitText:'提交并生效',onSubmit:async close=>{const form=byId('baseline-change-form');if(!form.reportValidity())throw new Error('请填写变更原因。');await api(`/internal/product-baselines/${id}/changes`,{method:'POST',body:JSON.stringify({changeReason:form.elements.changeReason.value,description:form.elements.description.value,vehicleModels:form.elements.vehicleModels.value,odd:form.elements.odd.value,capabilities:form.elements.capabilities.value,revision:b.revision})});close();toast('基础信息变更已生效。');await renderProductBaselines();}});}
