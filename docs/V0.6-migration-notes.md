# V0.6 Migration Notes

- Back up `data/deliverables.db` before replacing the executable.
- The first upgraded start adds the `Users` table and relation indexes without deleting existing data.
- When no users exist, create the first administrator in the browser.
- Existing deliverables, versions, changes and relations remain in the same SQLite database.
