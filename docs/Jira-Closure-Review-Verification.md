# Jira关闭复盘验证清单

## 统计边界

1. 为两个项目的同一严重等级设置不同关闭总周期（例如10天和21天），查询相同耗时的问题，确认按所属项目分别判定。
2. 修改时效标准后重新分析，确认卡片、对比图、明细一起应用新值；已保存的复盘快照保持原标准版本。
3. 验证耗时等于时限、超过时限1秒、缺少时限、缺少关闭日期和不完整历史。超期天数向上取整，按期判定不使用显示值的四舍五入结果。
4. 一条阶段超期但关闭总周期达标的问题，应算按期且允许复盘；完全未超期的问题不可复盘。
5. 多查询方案重复命中只计一次；同项目方案使用不同严重等级字段应在查询前纠正，保证统计定义一致。
6. 周/月/年、同比/环比、跨年ISO周、闰年、短月和当前半周期均检查分子、分母和样本量。基期零、空样本显示说明。
7. 关闭后重新打开、再次关闭和同一阶段多次进入，确认历史关闭事件与阶段累计时间正确。当前未关闭不应抹去早期周期末已关闭的记录。

## 复盘与权限

1. 首列为操作；筛选项目、严重等级、关闭时效、阶段、关闭日期和复盘状态；从明细进入复盘后保留列表筛选。
2. 空原因、空责任人、无分类不能保存；后台同样拒绝非法输入。供应商责任人可自由填写。
3. 保存后刷新、重新登录仍能查看；其他有查看权限的用户可查看共享记录；重复保存得到具体提示。
4. 两人同时修改同一复盘，旧修订号更新与删除均被拒绝。编辑不改写问题及标准快照。
5. 无查看权限无法访问列表；只读账号不能新增、编辑、删除；各写接口校验独立权限。字典分类选择不要求字典管理权限。
6. 切换Jira服务器后无法读取或修改其他服务器的复盘。直接请求接口无法绕过来源校验。
7. 被引用的分类无法删除；错误分类ID及其他字典的ID不能保存。删除复盘后引用计数减少。
8. 查看菜单三项分析与当前筛选后的列表一致；多阶段超期可贡献多条阶段统计，问题数量仍去重。
9. 校验CSV筛选结果、长文本转义、表格首列、移动端滚动、加载失败重试，以及PDF包含新增卡片和两张对比图。

## 本次已执行的验证范围

- Node统计回归、JavaScript语法及差异格式检查。
- SQLite从基础结构顺序执行迁移，并检查唯一约束、来源隔离、引用约束及修订号更新/删除。
- JSDOM＋模拟API交互验证：卡片/图表生成、已关闭筛选、必填校验、复盘新增/读取/修改/删除、修改后筛选项刷新和只读权限，通过且无未捕获JS异常。
- 当前环境没有.NET SDK，未执行.NET构建及真实Jira/ASP.NET联调。
- 已补充 Chromium 真浏览器验证，加载完整应用脚本与样式（模拟接口）：复盘增删改查、分类慢响应/失败/重试、取消未完成请求、保存失败保留输入、只读权限；普通/精简/已关闭三类穿透列表各121条记录，在1440×900、1280×720、1024×768、390×844、844×390窗口检查筛选、分页及可滚动布局。
- 已检查同比/环比和超期处理分析的浏览器截图；真实Jira、ASP.NET持久化联调及PDF实际打印仍需手动验证。

## 复盘交互与布局回归（本次修复）

- 复盘先显示窗口再读取分类，15秒未返回提供重试；窗口关闭后终止读取。无权限、历史不完整、未发现超期及状态加载失败均给出可见说明。
- 分类或保存失败在窗口内显示原因并保留已填内容；连续点击不重复打开或提交。
- 对比图查询条件、说明和图表具有独立间距；分析图卡片补齐内边距，长分类/责任人文本可换行。
- 当日未跟进图表在没有未关闭问题时也绑定穿透事件，点击零样本显示空明细。
- 共用穿透弹窗按可用空间布局，短窗口可滚动；列宽按标题/评论语义设置，首列操作和表头保持可访问。
- 静态资源版本已更新，避免新脚本搭配旧样式缓存。

浏览器回归脚本：`tests/jira-ui-regression.cjs`。在装有 Playwright 和 Chromium 的开发环境执行 `node tests/jira-ui-regression.cjs`。可通过 `PLAYWRIGHT_MODULE` 指定现有 Playwright 模块，通过 `CHROMIUM_EXECUTABLE_PATH` 指定浏览器；`JIRA_TEST_OUTPUT_DIR` 可选，用于保存截图。脚本启动临时静态服务，模拟业务接口，不启动.NET服务，不触发CI，不验证真实数据库写入。


## 按期穿透与阶段累计调整

- “按期关闭率（总周期）”仅穿透总周期达标的已关闭问题；清空筛选条件仍限定在该范围。总周期超期和无法判定的问题不进入该列表，“已关闭问题”入口继续展示全部已关闭问题。
- 已关闭明细、超期处理分析及对应CSV移除“是否关闭超期”列。
- “超期天数”统一累计各阶段已向上取整的超期天数，例如分析超期4天、修复超期3天，累计显示7天。阶段数据或标准不完整时显示“无法判定”，不使用总关闭周期超期天数代替。
- 验证通过：12项Node回归；Chromium模拟接口验证按期穿透范围、清空筛选、实际CSV下载、两处列表阶段累计显示，并通过现有复盘和多窗口布局回归。未执行.NET构建或真实Jira联调。


## 关闭时间与按期率口径修正

- 总览：确认按期数 / 所有已关闭问题数；无法判定留在分母。23条已关闭中20条按期、2条超期、1条未知，应显示87.0%。全未知且有已关闭问题显示0%，没有已关闭问题显示“—”。
- 关闭节点：以所配置关闭状态的最后一次状态操作时间为准；关闭状态A转关闭状态B取B的操作时间；重开再关闭取最新关闭操作。只有解决日期、没有关闭状态操作时间时，不推测关闭日期。
- 时效比较：使用原始时间差与当前项目、当前严重等级的关闭总周期比较；等于时限按期，超1秒不按期。阶段超期不改变总周期按期判定。
- 同比/环比：选周期末最新关闭操作，分母包含周期中能归档的全部已关闭样本；连续关闭状态之后重开，不能回退到更早关闭事件补入样本。无关闭操作时间的记录不能归档，在图表说明中提示。
- 修改Jira时效标准后重新分析；新增复盘使用服务端同一解析逻辑。已有复盘的审计快照保持保存时数据，不自动重写。
- 本次Node统计与浏览器模拟接口回归覆盖分母、未知样本、连续关闭/重开、跨周期、精度和卡片穿透；服务端关闭节点调整已做代码链路检查，未执行.NET构建或真实Jira接口验证。手动构建后请重点验证关闭A→关闭B、重开后再关闭、缺少操作历史及临界时限。

## 关闭效率柱线组合图

- 桌面两图左右排列，每周期左侧灰柱表示上一周期／去年同期，右侧蓝柱表示当期；橙色折线使用右轴。按期率折线＝当期按期率－基期按期率（百分点）；平均周期折线＝（当期－基期）÷基期（%）。
- 无有效样本显示“—”，折线在缺失周期断开；真实零值保留可穿透入口。平均周期基期为0时不计算变化率。
- 悬停或键盘聚焦展示两个周期的日期、值、按期／已关闭数量、未知数量和周期有效样本；Enter／空格打开所选柱子的对应周期明细，Escape收起提示。
- PDF保留左右布局，长范围每4个周期分段，同一指标的所有分段共用左右轴刻度。静态导出移除交互提示。
- 已通过19项Node回归，以及Chromium完整应用脚本／模拟接口回归：上下期柱子独立穿透、键盘提示、周／月／年与同比／环比切换、1440／1024／390宽度布局、原有复盘及各图穿透流程。已通过实际打印窗口生成PDF并检查16周期分段截图。
- 本次只调整图表展示与导出，已关闭识别、关闭状态操作时间、时效标准和按期率分母的统计代码保持原口径。未运行.NET构建、CI或真实Jira联调，由用户手动构建验证。


## JIRA看板统一迁移至ECharts

- 范围：12个主页面图表及穿透弹窗内2个分布图；保留卡片顺序、查询条件、时效统计、复盘和导出业务。“超期处理分析”页面没有迁移。
- 已移除手写SVG、CSS条形／渐变饼图、旧事件绑定及废弃样式。漏斗固定流程顺序、显示“创建”和阶段数量，清理原绘图中残留的环节转化展示与派生字段。
- Apache ECharts 6.0.0本地部署；从官方npm包提取并校验SHA-512，保留原始发行文件、LICENSE、NOTICE及SHA-256记录。
- 范围条使用ECharts过滤模式，单周期窗口不会把窗口外的其他周期压到同一坐标；纵轴采用全量范围刻度，PDF分段之间可以比较。
- 图表点击和键盘数据表共用同一穿透处理；空值不变成零，真正零值仍可通过数据表查看明细。详情筛选后两个分布图同步更新。
- 浏览器尺寸变化显式更新图表宽高；重绘、弹窗关闭及路由切换销毁实例。原始业务数据和后端权限／接口／数据库没有变更。
- 验证入口：`node --test tests/jira-closure.test.cjs`、`node tests/jira-ui-regression.cjs`。后者需Playwright及Chromium，可选输出截图与PDF；模拟业务API，不启动.NET服务或触发CI。
- 本次执行结果：20项Node统计／图表回归通过；Chromium加载完整应用脚本和本地ECharts，验证全部图形类型的原生点击、单双周期窗口、长人员列表缩放、筛选后的分布合计、1440／1024／390尺寸、反复重绘及页面移除后的实例释放，并通过既有复盘CRUD和穿透列表回归。已生成PDF并检查分段图表截图。没有执行.NET构建、CI或真实Jira联调，由用户手动构建验证。


## 字典驱动的问题分类

1. 启动升级后，“字典管理”应出现树形系统字典“JIRA问题分类”，大类为ADS、传感器，传感器下有毫米波雷达、激光雷达、摄像头。
2. 原始选项ADS／ADCU均归入ADS，LiDAR归入传感器／激光雷达。为其他实际选项补充名称或字段ID＋选项ID映射，重新分析后归类生效；选项ID映射在Jira选项改名后继续有效。
3. 新增、编辑、删除映射均保存成功并有审计；同字段相同映射冲突时整次保存回滚。父子循环与带子项删除仍被阻止。查看权限账号没有增删改入口，直接调用写接口也应拒绝。
4. 使用仅有JIRA看板查看权限的账号分析，不需要字典查看权限；多方案合并后问题按项目＋编号去重，分类分布之和等于未关闭问题总量。
5. 选择传感器后，分布图展示各细分项，处理人图包含所有传感器问题；选择叶子只统计该细分项。父级有直接映射时，本级问题单独列出，不遗漏或重复。
6. 点击分类图／处理人柱条，穿透明细显示正确范围；分类、严重等级、状态、处理人和关键词过滤影响弹窗图表、列表及CSV。CSV保留完整分类路径、Jira原始选项。
7. 未配置、新增源选项、空值及删除分类后重新分析的问题进入未分类；原始值可在未分类分布及明细中查看。无未关闭问题、空字典配置时不出现异常。
8. 选择某细分项后导出PDF，分类选择、饼图与处理人图均一致。桌面／窄屏布局正常，其他汇总及同比／环比不随本卡片选择变化。

自动化覆盖：25项Node统计／分类测试、5项SQLite存储迁移测试；浏览器回归覆盖分类穿透、CSV、PDF、映射表单增删改与只读入口（模拟API）。以上不替代.NET编译、真实鉴权和Jira服务端联调。
