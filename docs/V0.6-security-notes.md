# V0.6 Security Notes

- Passwords use salted PBKDF2-SHA256 hashes.
- Authentication uses an HTTP-only same-site cookie.
- Account disablement and role changes invalidate existing sessions.
- CSV export is restricted to authenticated users.
