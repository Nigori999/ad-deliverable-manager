# V0.6 Known Boundaries

- Hardware completeness currently uses the built-in standard hardware categories rather than a per-project configurable expected-deliverables list.
- Roles are fixed and cannot be customized.
- Large files remain on the file server; the application stores paths and metadata only.
